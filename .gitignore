# 비밀키 — 절대 커밋 금지 (CLAUDE.md 작업 규칙)
.env
.env.local
.env.*.local
.mcp.json

# Next.js
/node_modules
/.next
/out
/build
next-env.d.ts

# 기타
.DS_Store
*.log
npm-debug.log*
.vercel

# 개인 문서 (재무 정보 포함 — public 레포에 절대 커밋 금지)
/private/
